
 /*
 Modificadores de acesso são quem vai controlar a visibilidade dos atributos e métodos de uma classe.

 public: O modificador de acesso public torna o atributo ou método acessível de qualquer outra classe.

 default: O modificador de acesso default (quando nenhum modificador é especificado) torna o atributo ou método acessível apenas dentro do mesmo pacote. por exemplo apenas dentro do pacote 1-ModificadoresDeAcesso.

 private: O modificador de acesso private torna o atributo ou método acessível apenas dentro da própria classe.

 protected: O modificador de acesso protected torna o atributo ou método acessível dentro do mesmo pacote e também em subclasses, mesmo que estejam em pacotes diferentes.




 
  */


public class App {
    public static void main(String[] args) throws Exception {
        
    }
}
